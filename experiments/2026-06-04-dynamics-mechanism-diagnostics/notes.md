# Notes

This experiment should produce mechanism evidence, not just accuracy numbers.

The strongest paper story needs a plot or table showing that static feature
similarity is insufficient in the feature-similar cross-class regime, and that
training-dynamics signals recover useful residual ordering among edges with
similar feature cosine.

Important: `bad_edge_mask` may only be used after scores are computed, for
evaluation and diagnostics.
